# Batch
```rs
{{#include ./batch.rs}}
```
```rs
-- Batch Call --
At least one call has failed
-- Force Batch Call --
At least one call has failed
Batch completed even though one or more calls have failed.
```
